spring:
  datasource:
    url: jdbc:h2:mem:ticketing;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  flyway:
    schemas: ticketing
    default-schema: ticketing
